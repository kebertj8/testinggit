function miniMaxSum(arr) {
  let arr = []
  let arr2 = arr - arr.min
  for (let i = 0; i < arr.length; i++) {
    arr += arr.reduce[i]
    arr2 += arr2.reduce[i]
    
  }
  return Math.abs(arr)
  return Math.abs(arr2)
}



// define the variables
// 2 arrays
// 1 that takes away the first or smallest indices
// count each
// var array using.max etc
